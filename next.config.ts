import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com', 'images.unsplash.com'], // Add more domains as needed
    },
};

export default nextConfig;
